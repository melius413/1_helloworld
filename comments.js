// 노드실행은 콘솔창에서 'node app.js' 또는 'node app'
// 노드 Doc
// https://nodejs.org/dist/latest-v12.x/docs/api/

// npmjs.com: node package manager
// js 모듈이 80만개정도됨
// 보통 프론트는 npm 다운 없이 cdn에서 링크걸어 사용한다.
// lodash
// npm, popularity... 인기순

// npm init
// > 깃이 연동되어 있으면 git 허브주소 자동으로 찾아준다.
// > package.json 의존성 관리 객체

// npm init -y      // 모든 질문은 yes로 처리
// npm i express    // express 설치
// index.js가 모듈의 진입점

// 각 모듈은 서로를 이용해서 구현한다. dependency(의존성)

// npm i // package.json만 있으면 해당 모듈 재설치 가능

// ctrl + j, 터미널 창 열림

// 교재...???
// git clone {복사 주소}

// 크롬 웹스토어에서 json viewer 설치
